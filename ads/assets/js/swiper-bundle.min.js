document.addEventListener("DOMContentLoaded", function () {
    var parent = document.querySelector(".myBtnContainer");
    parent.addEventListener("click", function (event) {
        // Change active button class
        // Array.from(parent.children).forEach(function (currentValue) {
        //     currentValue.classList.remove("text-white");
        // });
        document
            .getElementsByClassName("filterButton text-white")[0]
            .classList.remove("text-white");

        event.target.classList.add("text-white");

        // Set slide filter by class name
        Array.from(document.querySelectorAll(".swiper-slide")).forEach(
            function (currentValue) {
                if (
                    (event.target.innerText.toLowerCase() ||
                        event.target.textContent.toLowerCase()) == "all"
                ) {
                    currentValue.classList.remove("hidden");
                } else {
                    if (
                        !currentValue.classList.contains(
                            event.target.innerText.toLowerCase() ||
                                event.target.textContent.toLowerCase()
                        )
                    ) {
                        currentValue.classList.add("hidden");
                        // console.log(allSlides)
                    } else {
                        currentValue.classList.remove("hidden");
                    }
                }
                mySwiper.update();
                mySwiper.updateSize();
                mySwiper.updateSlides();
                mySwiper.updateSlidesClasses();
                mySwiper.slideTo(0);

            }
        );
    });
});

document.addEventListener("DOMContentLoaded", function () {
    var parent02 = document.querySelector(".myBtnContainer");
    parent02.addEventListener("click", function (event) {
        // Change active button class
        // Array.from(parent.children).forEach(function (currentValue) {
        //     currentValue.classList.remove("text-white");
        // });
        document
            .getElementsByClassName("filterButton text-white")[0]
            .classList.remove("text-white");

        event.target.classList.add("text-white");

        // Set slide filter by class name
        Array.from(document.querySelectorAll(".swiper-slide")).forEach(
            function (currentValue) {
                if (
                    (event.target.innerText.toLowerCase() ||
                        event.target.textContent.toLowerCase()) == "all"
                ) {
                    currentValue.classList.remove("hidden");
                } else {
                    if (
                        !currentValue.classList.contains(
                            event.target.innerText.toLowerCase() ||
                                event.target.textContent.toLowerCase()
                        )
                    ) {
                        currentValue.classList.add("hidden");
                        // console.log(allSlides)
                    } else {
                        currentValue.classList.remove("hidden");
                    }
                }
				
				mySwiper02.update();
                mySwiper02.updateSize();
                mySwiper02.updateSlides();
                mySwiper02.updateSlidesClasses();
                mySwiper02.slideTo(0);
            }
        );
    });
});

document.addEventListener("DOMContentLoaded", function () {
    var parent03 = document.querySelector(".myBtnContainer");
    parent03.addEventListener("click", function (event) {
        // Change active button class
        // Array.from(parent.children).forEach(function (currentValue) {
        //     currentValue.classList.remove("text-white");
        // });
        document
            .getElementsByClassName("filterButton text-white")[0]
            .classList.remove("text-white");

        event.target.classList.add("text-white");

        // Set slide filter by class name
        Array.from(document.querySelectorAll(".swiper-slide")).forEach(
            function (currentValue) {
                if (
                    (event.target.innerText.toLowerCase() ||
                        event.target.textContent.toLowerCase()) == "all"
                ) {
                    currentValue.classList.remove("hidden");
                } else {
                    if (
                        !currentValue.classList.contains(
                            event.target.innerText.toLowerCase() ||
                                event.target.textContent.toLowerCase()
                        )
                    ) {
                        currentValue.classList.add("hidden");
                        // console.log(allSlides)
                    } else {
                        currentValue.classList.remove("hidden");
                    }
                }
				
				mySwiper03.update();
                mySwiper03.updateSize();
                mySwiper03.updateSlides();
                mySwiper03.updateSlidesClasses();
                mySwiper03.slideTo(0);
            }
        );
    });
});

var mySwiper = new Swiper(".swiper-container", {
    // loop: true,
    navigation: {
        nextEl: ".swiper-button-next",
        prevEl: ".swiper-button-prev"
    },
    breakpoints: {
        640: {
            slidesPerView: 1,
            spaceBetween: 0
        },
        768: {
            slidesPerView: 2,
            spaceBetween: 10
        },
        1024: {
            slidesPerView: 3,
            spaceBetween: 20
        }
        // 1280: {
        //     slidesPerView: 4,
        //     spaceBetween: 20
        // }
    }
});

var mySwiper02 = new Swiper(".swiper-container02", {
    // loop: true,
    navigation: {
        nextEl: ".swiper-button-next",
        prevEl: ".swiper-button-prev"
    },
    breakpoints: {
        640: {
            slidesPerView: 1,
            spaceBetween: 0
        },
        768: {
            slidesPerView: 3,
            spaceBetween: 10
        },
        1024: {
            slidesPerView: 5,
            spaceBetween: 20
        }
        // 1280: {
        //     slidesPerView: 4,
        //     spaceBetween: 20
        // }
    }
});

var mySwiper03 = new Swiper(".swiper-container03", {
    slidesPerView: 1,
        
        
        pagination: {
          el: ".swiper-pagination",
          clickable: true,
        },
        navigation: {
          nextEl: ".swiper-button-next",
          prevEl: ".swiper-button-prev",
        },
	breakpoints: {
        640: {
            slidesPerView: 1,
            spaceBetween: 0
        },
        768: {
            slidesPerView: 1,
            spaceBetween: 0 
        },
        1024: {
            slidesPerView: 1,
            spaceBetween: 0
        }
        // 1280: {
        //     slidesPerView: 4,
        //     spaceBetween: 20
        // }
    }
});

var mySwiper04 = new Swiper(".swiper-container04", {
    slidesPerView: 1,
        
        
        pagination: {
          el: ".swiper-pagination",
          clickable: true,
        },
        navigation: {
          nextEl: ".swiper-button-next",
          prevEl: ".swiper-button-prev",
        },
	breakpoints: {
        640: {
            slidesPerView: 1,
            spaceBetween: 0
        },
        768: {
            slidesPerView: 1,
            spaceBetween: 0 
        },
        1024: {
            slidesPerView: 1,
            spaceBetween: 0
        }
        // 1280: {
        //     slidesPerView: 4,
        //     spaceBetween: 20
        // }
    }
});
